<h1>Listing Articles</h1>
 <%= link_to 'Create Article', new_article_path %><br>
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>Commenter</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td>
      <%# Return JSON Object on screen by adding (<%= article.comments.each) %>
        <%= article.comments.each do |comment| %>
          <%= comment.commenter%><br>
        <% end %>
      </td>
      <%# Details link %>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Destroy', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
<hr />
<h1>SQL Manipulation Practice</h1>
<div>
    <h3>Article Data</h3>
      <%= @articles.each do |article| %>
      <% end %>
</div>

<div>
    <h3>Comment Data</h3>
    <% @articles.each do |article| %>
        <%= article.comments.each do |comment| %>
        <% end %>
    <% end %>
</div>